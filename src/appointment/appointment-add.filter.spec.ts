import { AppointmentAddFilter } from './appointment-add.filter';

describe('AppointmentAddFilter', () => {
  it('should be defined', () => {
    expect(new AppointmentAddFilter()).toBeDefined();
  });
});
